# sn-test-front
SN Test (Front-end)

## Create react app

```bash
~$ npx create-react-app sn-test-front
```

## Dependencies

```bash
~$ sudo npm install -g n
~$ sudo n stable
~$ sudo npm install --global yarn
~$ yarn install
```
